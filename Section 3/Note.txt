create component with cli
template vs template url
styling - bootstrap
component selector (tag - attribute - class)

data binding
{{ }}
property binding
(click)
(input)="onInput($event)"
data emitted with this event
event: Event
(<HTMLInputElement>event.target).value

[(ngModel)]

string interpolation
property binding settimeout

event binding print (created)
event binding input ($event)
2 way binding (new input - ngModel)

directives instructures in the dom
angualr please add component
directives without template (change in the dom)


structure directive: add or remove elements in the dom
attribute directive: change the element they where palced on
if statement (structure directive)
<ng-template>  local reference 
[ngStyle]="{'color': condition}"
[ngClass]="{class_name: contition}"

*ngFor="let x of list; let i=index"